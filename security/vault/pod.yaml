apiVersion: v1
kind: Pod
metadata:
  name: test
spec:
  serviceAccountName: vault-auth
  containers:
  - name: test
    image: alpine:3.2
    command:
      - /bin/sh
      - "-c"
      - "sleep 60m"
    imagePullPolicy: IfNotPresent